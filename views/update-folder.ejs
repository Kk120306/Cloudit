<!DOCTYPE html>
<html lang="en" class="bg-black">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Update Folder</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen flex items-center justify-center px-6 py-12 font-sans text-gray-200">

    <div class="bg-gray-900 bg-gradient-to-br from-gray-900 to-gray-800 p-10 rounded-lg shadow-xl max-w-md w-full">
        <h1 class="text-3xl font-semibold mb-6 select-none text-white">Update Folder</h1>

        <form action="<%= actionPath %>" method="POST" class="flex flex-col gap-6">

            <div>
                <label for="name" class="block mb-2 text-gray-300 font-medium">Folder Name</label>
                <input id="name" name="name" type="text" placeholder="Folder Name" required
                    class="w-full bg-gray-800 text-gray-200 rounded-md border border-gray-700 p-3 focus:outline-none focus:ring-2 focus:ring-blue-600 transition" />
                <% const nameError=errors?.find(e=> e.path === 'name'); %>
                    <% if (nameError) { %>
                        <p class="mt-2 text-red-600 text-sm font-medium select-none">
                            <%= nameError.msg %>
                        </p>
                        <% } %>
            </div>

            <button type="submit"
                class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 rounded-md transition">
                Update Folder
            </button>

        </form>

        <a href="/library" class="mt-6 block text-center text-gray-400 hover:text-gray-200 transition underline">
            Back to Library
        </a>
    </div>

</body>

</html>