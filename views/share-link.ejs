<!DOCTYPE html>
<html lang="en" class="bg-black">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Shareable Link Created</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen flex items-center justify-center px-6 py-12 font-sans text-gray-200">

    <div
        class="bg-gray-900 bg-gradient-to-br from-gray-900 to-gray-800 p-10 rounded-lg shadow-xl max-w-lg w-full text-center">
        <h2 class="text-3xl font-semibold mb-4 select-none text-white">Shareable Link Created!</h2>
        <p class="mb-6 text-gray-400">Anyone with this link can access the folder:</p>

        <input type="text" id="shareLink" value="<%= shareUrl %>" readonly
            class="w-full p-3 rounded-md bg-gray-800 text-gray-200 border border-gray-700 mb-6 select-all cursor-pointer"
            onclick="this.select()" />

        <button onclick="copyLink()"
            class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 px-6 rounded-md transition mb-6">
            Copy Link
        </button>

        <br />
        <a href="/library" class="text-gray-400 hover:text-gray-200 transition underline">
            Back to Home
        </a>
    </div>

    <script>
        function copyLink() {
            const input = document.getElementById('shareLink');
            input.select();
            input.setSelectionRange(0, 99999);
            navigator.clipboard.writeText(input.value).then(() => {
                alert("Link copied to clipboard!");
            });
        }
    </script>

</body>

</html>